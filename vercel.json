{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "Fallback for when the Vercel project's Root Directory is the repo root. Preferred: set Root Directory to apps/web (then apps/web/vercel.json applies and this file is ignored). See docs/runbooks/deploy-vercel-web.md.",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --no-frozen-lockfile --filter=@nexahaus/web...",
  "buildCommand": "corepack enable && pnpm --filter=@nexahaus/web... build",
  "outputDirectory": "apps/web/.next"
}
