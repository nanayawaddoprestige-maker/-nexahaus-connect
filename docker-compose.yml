# Local development infrastructure for NexaHaus Connect.
# App processes (api, web, worker) normally run via `pnpm dev`; this file
# provides the backing services. See docs/DEPLOYMENT.md.

services:
  postgres:
    image: postgres:16-alpine
    container_name: nexahaus-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: nexahaus
      POSTGRES_PASSWORD: nexahaus
      POSTGRES_DB: nexahaus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexahaus -d nexahaus"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: nexahaus-redis
    restart: unless-stopped
    command: ["redis-server", "--save", "60", "1", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    container_name: nexahaus-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10

  createbuckets:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin;
      mc mb --ignore-existing local/nexahaus-documents;
      mc anonymous set none local/nexahaus-documents;
      mc version enable local/nexahaus-documents;
      exit 0;
      "

volumes:
  pgdata:
  redisdata:
  miniodata:
